# 协作规则

## 开始任务前优先读取

处理本项目任务前，优先读取：

1. `docs/PROJECT_CONTEXT.md`：项目上下文、技术栈、目录结构、常用命令、关键模块和已知注意事项。
2. 与当前任务直接相关的文件。

除非遇到以下情况，不要每次重新全量探索项目上下文：

- `docs/PROJECT_CONTEXT.md` 缺失或明显过期。
- `package.json`、`vite.config.js`、后端入口或目录结构发生明显变化。
- 用户明确要求重新梳理项目结构、技术栈或近期变更。
- 当前问题无法通过上下文文件和相关文件定位。

## 修改后同步维护

如果本次修改影响以下内容，请同步更新 `docs/PROJECT_CONTEXT.md`：

- 技术栈、依赖或运行命令。
- 关键目录或模块职责。
- 重要接口、数据结构或数据兼容规则。
- 测试/构建方式。
- 已知问题、踩坑记录或回归测试位置。

## 开发约束

- 优先做最小化修改，避免影响原有内容。
- 代码、命令、报错日志保持原样。
- 回答用户时使用简体中文。
- 修改前注意当前工作区可能已有未提交改动，不要覆盖无关文件。
